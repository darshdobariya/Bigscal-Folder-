<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Test2" parent="Theme.Material3.DayNight.NoActionBar">
    </style>

    <style name="Theme.Test2" parent="Base.Theme.Test2" />

    <!-- Defined a new style with three items of color. -->
    <style name="ourAppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/orange</item>

        <!-- Defining that new color in ColorPrimaryDark -->
        <item name="colorPrimaryDark">@color/orange_light</item>
        <item name="colorAccent">@color/orange</item>
    </style>
</resources>